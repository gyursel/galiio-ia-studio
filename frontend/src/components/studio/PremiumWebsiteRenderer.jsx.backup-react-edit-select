import React from "react";

function text(value, fallback = "") {
  const v = String(value || "").trim();
  return v || fallback;
}

function list(value, fallback = []) {
  return Array.isArray(value) && value.length ? value : fallback;
}

export default function PremiumWebsiteRenderer({ website }) {
  const state = website || {};

  const brandName = text(state.brandName, "Galio Premium");
  const tagline = text(state.tagline, "Premium digital experience");
  const industry = text(state.industry, "premium brand");
  const primary = text(state.primaryColor, "#7CFFB2");
  const secondary = text(state.secondaryColor, "#07070a");

  const hero = state.hero || {};
  const footer = state.footer || {};
  const media = state.mediaAssets || {};
  const heroVideoUrl = text(media.heroVideoUrl || media.backgroundVideoUrl, "");
  const heroPosterUrl = text(media.heroPosterUrl || media.heroImageUrl || media.backgroundImageUrl, "");
  const collectionImages = list(media.collectionImages, []);

  const features = list(state.features, [
    {
      title: "Premium Strategy",
      description: "A polished structure built for trust, conversion and high-end brand perception.",
    },
    {
      title: "Cinematic Visuals",
      description: "Dark, modern, memorable presentation with strong hierarchy and premium spacing.",
    },
    {
      title: "Responsive Flow",
      description: "Clean sections designed to feel sharp across desktop, tablet and mobile.",
    },
  ]);

  const sections = list(state.sections, [
    {
      title: "Signature Experience",
      description: `A carefully crafted presentation for ${industry}, focused on trust, clarity and premium perception.`,
      items: ["Premium positioning", "Clear visual hierarchy", "Conversion-first layout"],
    },
    {
      title: "Built To Convert",
      description: "Every section guides visitors from first impression to action with strong messaging and elegant UI.",
      items: ["Hero CTA", "Product highlights", "Trust signals"],
    },
  ]);

  const pricing = list(state.pricing, []);

  return (
    <div
      className="gpr-site"
      style={{
        "--gpr-primary": primary,
        "--gpr-secondary": secondary,
      }}
    >
      {(heroVideoUrl || heroPosterUrl) && (
        <div className="gpr-media-bg">
          {heroVideoUrl ? (
            <video src={heroVideoUrl} poster={heroPosterUrl} autoPlay muted loop playsInline />
          ) : (
            <img src={heroPosterUrl} alt="" />
          )}
        </div>
      )}

      <style>{`
        .gpr-site {
          min-height: 100%;
          color: #f8fafc;
          background:
            radial-gradient(circle at 18% 10%, color-mix(in srgb, var(--gpr-primary) 28%, transparent), transparent 32%),
            radial-gradient(circle at 85% 15%, rgba(96,165,250,.18), transparent 30%),
            linear-gradient(135deg, #050509 0%, #0b0f18 52%, #050509 100%);
          font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
          overflow: hidden;
        }
        .gpr-wrap {
          width: min(1180px, calc(100% - 32px));
          margin: 0 auto;
        }
        .gpr-nav {
          padding: 22px 0;
          display: flex;
          align-items: center;
          justify-content: space-between;
        }
        .gpr-logo {
          display: flex;
          align-items: center;
          gap: 10px;
          font-weight: 900;
          letter-spacing: -.04em;
        }
        .gpr-logo-mark {
          width: 18px;
          height: 18px;
          border-radius: 7px;
          background: var(--gpr-primary);
          box-shadow: 0 0 28px color-mix(in srgb, var(--gpr-primary) 65%, transparent);
        }
        .gpr-nav-links {
          display: flex;
          gap: 24px;
          color: rgba(248,250,252,.68);
          font-size: 14px;
        }
        .gpr-btn {
          border: 0;
          border-radius: 999px;
          background: var(--gpr-primary);
          color: #04110b;
          padding: 13px 19px;
          font-weight: 900;
          box-shadow: 0 18px 55px color-mix(in srgb, var(--gpr-primary) 28%, transparent);
        }
        .gpr-btn-secondary {
          color: #f8fafc;
          background: rgba(255,255,255,.075);
          border: 1px solid rgba(255,255,255,.12);
          box-shadow: none;
        }
        .gpr-media-bg {
          position: absolute;
          inset: 0;
          overflow: hidden;
          pointer-events: none;
          opacity: .34;
        }
        .gpr-media-bg video,
        .gpr-media-bg img {
          width: 100%;
          height: 100%;
          object-fit: cover;
          filter: saturate(1.08) contrast(1.12);
        }
        .gpr-media-bg::after {
          content: "";
          position: absolute;
          inset: 0;
          background:
            radial-gradient(circle at 20% 20%, color-mix(in srgb, var(--gpr-primary) 22%, transparent), transparent 32%),
            linear-gradient(90deg, rgba(5,5,9,.96), rgba(5,5,9,.72), rgba(5,5,9,.94));
        }
        .gpr-site {
          position: relative;
        }
        .gpr-wrap {
          position: relative;
          z-index: 1;
        }
        .gpr-hero {
          min-height: 720px;
          display: grid;
          grid-template-columns: 1.05fr .95fr;
          gap: 54px;
          align-items: center;
          padding: 54px 0 78px;
        }
        .gpr-eyebrow {
          width: fit-content;
          border: 1px solid rgba(255,255,255,.12);
          background: rgba(255,255,255,.06);
          color: var(--gpr-primary);
          border-radius: 999px;
          padding: 9px 14px;
          margin-bottom: 22px;
          font-size: 13px;
          font-weight: 800;
        }
        .gpr-hero h1 {
          max-width: 760px;
          margin: 0;
          font-size: clamp(48px, 7.6vw, 104px);
          line-height: .88;
          letter-spacing: -.085em;
        }
        .gpr-hero p {
          max-width: 680px;
          color: rgba(248,250,252,.68);
          font-size: 19px;
          line-height: 1.72;
          margin: 26px 0 0;
        }
        .gpr-actions {
          display: flex;
          gap: 14px;
          margin-top: 32px;
        }
        .gpr-stats {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 12px;
          margin-top: 42px;
          max-width: 620px;
        }
        .gpr-stats div,
        .gpr-card,
        .gpr-section-card,
        .gpr-price-card {
          border: 1px solid rgba(255,255,255,.12);
          background: rgba(255,255,255,.055);
          border-radius: 30px;
          box-shadow: 0 24px 80px rgba(0,0,0,.18);
        }
        .gpr-stats div {
          padding: 18px;
        }
        .gpr-stats strong {
          display: block;
          font-size: 22px;
        }
        .gpr-stats span {
          display: block;
          color: rgba(248,250,252,.68);
          font-size: 13px;
          margin-top: 4px;
        }
        .gpr-visual {
          position: relative;
          min-height: 580px;
          display: grid;
          place-items: center;
        }
        .gpr-device {
          position: relative;
          z-index: 2;
          width: min(390px, 88vw);
          min-height: 530px;
          border-radius: 46px;
          border: 1px solid rgba(255,255,255,.2);
          background:
            linear-gradient(180deg, rgba(255,255,255,.13), rgba(255,255,255,.035)),
            radial-gradient(circle at 50% 0%, color-mix(in srgb, var(--gpr-primary) 26%, transparent), transparent 42%);
          box-shadow: 0 42px 120px rgba(0,0,0,.55);
          padding: 22px;
          backdrop-filter: blur(22px);
        }
        .gpr-notch {
          width: 92px;
          height: 8px;
          border-radius: 999px;
          margin: 0 auto 52px;
          background: rgba(255,255,255,.22);
        }
        .gpr-screen {
          border-radius: 32px;
          border: 1px solid rgba(255,255,255,.12);
          background: rgba(0,0,0,.34);
          padding: 26px;
        }
        .gpr-screen span {
          color: var(--gpr-primary);
          font-size: 12px;
          font-weight: 900;
          text-transform: uppercase;
          letter-spacing: .12em;
        }
        .gpr-screen strong {
          display: block;
          font-size: 38px;
          letter-spacing: -.06em;
          line-height: .95;
          margin-top: 18px;
        }
        .gpr-screen p {
          color: rgba(248,250,252,.68);
          font-size: 14px;
          line-height: 1.55;
          margin-top: 18px;
        }
        .gpr-products {
          display: grid;
          grid-template-columns: repeat(3, 1fr);
          gap: 12px;
          margin-top: 18px;
        }
        .gpr-products div {
          height: 118px;
          border-radius: 24px;
          background:
            linear-gradient(180deg, color-mix(in srgb, var(--gpr-primary) 28%, transparent), rgba(255,255,255,.06)),
            rgba(255,255,255,.06);
          border: 1px solid rgba(255,255,255,.12);
          overflow: hidden;
        }
        .gpr-products img {
          width: 100%;
          height: 100%;
          object-fit: cover;
          opacity: .86;
        }
        .gpr-gallery {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 18px;
          padding: 0 0 80px;
        }
        .gpr-gallery-card {
          min-height: 260px;
          border-radius: 30px;
          overflow: hidden;
          border: 1px solid rgba(255,255,255,.12);
          background: rgba(255,255,255,.055);
          position: relative;
          box-shadow: 0 24px 80px rgba(0,0,0,.18);
        }
        .gpr-gallery-card img {
          width: 100%;
          height: 100%;
          min-height: 260px;
          object-fit: cover;
          filter: saturate(1.08) contrast(1.08);
        }
        .gpr-gallery-info {
          position: absolute;
          left: 18px;
          right: 18px;
          bottom: 18px;
          border: 1px solid rgba(255,255,255,.14);
          border-radius: 22px;
          padding: 16px;
          background: rgba(0,0,0,.48);
          backdrop-filter: blur(18px);
        }
        .gpr-gallery-info span {
          color: var(--gpr-primary);
          font-size: 11px;
          font-weight: 900;
          text-transform: uppercase;
          letter-spacing: .12em;
        }
        .gpr-gallery-info strong {
          display: block;
          margin-top: 6px;
          font-size: 18px;
        }
        .gpr-orb {
          position: absolute;
          border-radius: 999px;
          filter: blur(8px);
        }
        .gpr-orb-a {
          width: 230px;
          height: 230px;
          right: 14px;
          top: 48px;
          background: color-mix(in srgb, var(--gpr-primary) 24%, transparent);
        }
        .gpr-orb-b {
          width: 160px;
          height: 160px;
          left: 22px;
          bottom: 70px;
          background: rgba(96,165,250,.2);
        }
        .gpr-features,
        .gpr-pricing-grid {
          display: grid;
          grid-template-columns: repeat(3, minmax(0, 1fr));
          gap: 18px;
          padding: 24px 0 80px;
        }
        .gpr-card,
        .gpr-section-card,
        .gpr-price-card {
          padding: 28px;
        }
        .gpr-icon {
          width: 42px;
          height: 42px;
          border-radius: 16px;
          background: var(--gpr-primary);
          box-shadow: 0 0 38px color-mix(in srgb, var(--gpr-primary) 45%, transparent);
          margin-bottom: 20px;
        }
        .gpr-card h3,
        .gpr-section-card h3,
        .gpr-price-card h3 {
          margin: 0;
          font-size: 24px;
          letter-spacing: -.04em;
        }
        .gpr-card p,
        .gpr-section-card p,
        .gpr-price-card p,
        .gpr-price-card li {
          color: rgba(248,250,252,.68);
          line-height: 1.65;
        }
        .gpr-section-head {
          margin-bottom: 24px;
        }
        .gpr-section-head p,
        .gpr-cta p {
          color: var(--gpr-primary);
          text-transform: uppercase;
          letter-spacing: .12em;
          font-size: 13px;
          font-weight: 900;
        }
        .gpr-section-head h2,
        .gpr-cta h2 {
          max-width: 790px;
          margin: 0;
          font-size: clamp(34px, 5vw, 68px);
          line-height: .95;
          letter-spacing: -.07em;
        }
        .gpr-section-grid {
          display: grid;
          grid-template-columns: repeat(2, minmax(0, 1fr));
          gap: 18px;
          padding-bottom: 80px;
        }
        .gpr-section-card-large {
          grid-row: span 2;
        }
        .gpr-number {
          color: var(--gpr-primary);
          font-weight: 900;
          margin-bottom: 28px;
        }
        .gpr-chip-row {
          display: flex;
          gap: 10px;
          flex-wrap: wrap;
          margin-top: 22px;
        }
        .gpr-chip-row span {
          border: 1px solid rgba(255,255,255,.12);
          border-radius: 999px;
          padding: 8px 11px;
          color: rgba(255,255,255,.78);
          background: rgba(255,255,255,.045);
          font-size: 13px;
        }
        .gpr-cta {
          margin: 0 auto 40px;
          border: 1px solid rgba(255,255,255,.12);
          border-radius: 38px;
          padding: 56px;
          background:
            radial-gradient(circle at 80% 20%, color-mix(in srgb, var(--gpr-primary) 22%, transparent), transparent 38%),
            rgba(255,255,255,.055);
        }
        .gpr-cta button {
          margin-top: 26px;
        }
        .gpr-footer {
          padding: 32px 0 48px;
          display: flex;
          justify-content: space-between;
          color: rgba(248,250,252,.68);
          border-top: 1px solid rgba(255,255,255,.12);
        }
        @media (max-width: 900px) {
          .gpr-nav-links { display: none; }
          .gpr-hero { grid-template-columns: 1fr; min-height: auto; }
          .gpr-features,
          .gpr-pricing-grid,
          .gpr-section-grid,
          .gpr-gallery,
          .gpr-stats { grid-template-columns: 1fr; }
          .gpr-hero h1 { font-size: 52px; }
          .gpr-cta { padding: 32px; }
        }
      `}</style>

      <div className="gpr-wrap">
        <nav className="gpr-nav">
          <div className="gpr-logo">
            <span className="gpr-logo-mark" />
            {brandName}
          </div>
          <div className="gpr-nav-links">
            <span>Experience</span>
            <span>Features</span>
            <span>Work</span>
            <span>Contact</span>
          </div>
          <button className="gpr-btn">{text(hero.primaryCta, "Get Started")}</button>
        </nav>

        <section className="gpr-hero">
          <div>
            <div className="gpr-eyebrow">{text(hero.eyebrow, industry)}</div>
            <h1>{text(hero.headline, `Premium ${brandName}`)}</h1>
            <p>{text(hero.subheadline, tagline)}</p>
            <div className="gpr-actions">
              <button className="gpr-btn">{text(hero.primaryCta, "Get Started")}</button>
              <button className="gpr-btn gpr-btn-secondary">{text(hero.secondaryCta, "Explore")}</button>
            </div>
            <div className="gpr-stats">
              <div><strong>Premium</strong><span>Visual system</span></div>
              <div><strong>Fast</strong><span>Launch flow</span></div>
              <div><strong>100%</strong><span>Responsive</span></div>
            </div>
          </div>

          <div className="gpr-visual">
            <div className="gpr-device">
              <div className="gpr-notch" />
              <div className="gpr-screen">
                <span>{industry}</span>
                <strong>{brandName}</strong>
                <p>{tagline}</p>
              </div>
              <div className="gpr-products">
                {[0, 1, 2].map((idx) => (
                  <div key={`mini-${idx}`}>
                    {collectionImages[idx]?.imageUrl ? (
                      <img src={collectionImages[idx].imageUrl} alt={collectionImages[idx].title || ""} />
                    ) : null}
                  </div>
                ))}
              </div>
            </div>
            <div className="gpr-orb gpr-orb-a" />
            <div className="gpr-orb gpr-orb-b" />
          </div>
        </section>

        <section className="gpr-features">
          {features.slice(0, 6).map((item, index) => (
            <article className="gpr-card" key={`feature-${index}`}>
              <div className="gpr-icon" />
              <h3>{text(item.title, "Premium Feature")}</h3>
              <p>{text(item.description, "Premium section description.")}</p>
            </article>
          ))}
        </section>

        <section>
          <div className="gpr-section-head">
            <p>Experience Architecture</p>
            <h2>Designed like a premium product launch</h2>
          </div>
          <div className="gpr-section-grid">
            {sections.slice(0, 4).map((section, index) => (
              <article
                className={`gpr-section-card ${index === 0 ? "gpr-section-card-large" : ""}`}
                key={`section-${index}`}
              >
                <div className="gpr-number">0{index + 1}</div>
                <h3>{text(section.title, "Premium Section")}</h3>
                <p>{text(section.description, "Premium section copy.")}</p>
                <div className="gpr-chip-row">
                  {list(section.items, ["Premium", "Modern", "Responsive"]).slice(0, 5).map((item, idx) => (
                    <span key={`chip-${index}-${idx}`}>{item}</span>
                  ))}
                </div>
              </article>
            ))}
          </div>
        </section>

        {collectionImages.length > 0 && (
          <section>
            <div className="gpr-section-head">
              <p>Visual Direction</p>
              <h2>Real media matched to the project subject</h2>
            </div>
            <div className="gpr-gallery">
              {collectionImages.slice(0, 3).map((item, index) => (
                <article className="gpr-gallery-card" key={`gallery-${index}`}>
                  <img src={item.imageUrl} alt={item.title || ""} />
                  <div className="gpr-gallery-info">
                    <span>{item.tag || "Premium"}</span>
                    <strong>{item.subtitle || item.title || industry}</strong>
                  </div>
                </article>
              ))}
            </div>
          </section>
        )}

        {pricing.length > 0 && (
          <section>
            <div className="gpr-section-head">
              <p>Offers</p>
              <h2>Choose the right experience</h2>
            </div>
            <div className="gpr-pricing-grid">
              {pricing.slice(0, 3).map((plan, index) => (
                <article className="gpr-price-card" key={`price-${index}`}>
                  <p>{text(plan.name, "Plan")}</p>
                  <h3>{text(plan.price, "Custom")}</h3>
                  <p>{text(plan.description, "Premium offer.")}</p>
                  <ul>
                    {list(plan.features, ["Premium design", "Responsive build", "Launch ready"]).slice(0, 5).map((f, i) => (
                      <li key={`pf-${index}-${i}`}>{f}</li>
                    ))}
                  </ul>
                </article>
              ))}
            </div>
          </section>
        )}

        <section className="gpr-cta">
          <p>{industry}</p>
          <h2>{text(footer.headline, `Ready to launch ${brandName}?`)}</h2>
          <button className="gpr-btn">{text(footer.cta, text(hero.primaryCta, "Start Now"))}</button>
        </section>

        <footer className="gpr-footer">
          <span>{brandName}</span>
          <span>{tagline}</span>
        </footer>
      </div>
    </div>
  );
}
